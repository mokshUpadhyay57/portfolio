[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["netlify/functions/*.ttf"]

[[edge_functions]]
  path = "/projects/*"
  function = "inject-seo"
